<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="isauze.ma_super_appli_scolaire">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:name="com.orm.SugarApp">
        <activity android:name=".Accueil"
            >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".creer_compte" />
        <activity android:name=".validation_compte" />
        <activity android:name=".liste_exercices"></activity>

    <!-- SugarORM -->
    <metadata
        android:name="DATABASE"
        android:value="ma_base.db"
        />
    <metadata
        android:name="VERSION"
        android:value="1"
        />
    <metadata
        android:name="QUERY_LOG"
        android:value="true"
        />
    <metadata
        android:name="DOMAIN_PACKAGE_NAME"
        android:value="mi1.ma_base.data"
        />
    </application>

</manifest>