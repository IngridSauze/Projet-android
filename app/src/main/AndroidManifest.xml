<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="isauze.ma_super_appli_scolaire">

    <application
        android:name="com.orm.SugarApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <!-- SugarORM -->
        <meta-data
            android:name="DATABASE"
            android:value="yolooh.db" />
        <meta-data
            android:name="VERSION"
            android:value="3" />
        <meta-data
            android:name="QUERY_LOG"
            android:value="true" />
        <meta-data
            android:name="DOMAIN_PACKAGE_NAME"
            android:value="classes_base" />

        <activity android:name=".Accueil">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".creer_compte" />
        <activity android:name=".validation_compte" />
        <activity android:name=".liste_exercices" />
        <activity android:name=".maths_accueil" />
        <activity android:name=".addition" />
        <activity android:name=".addition_1" />
        <activity android:name=".addition_2"></activity>
    </application>

</manifest>